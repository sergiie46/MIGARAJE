# Mi Garaje V3 - correcciones de compilación

- Corregidas las 3 reglas Lint inválidas de backup que usaban `domain="database" path="firestore/"`.
- `backup_rules.xml`: la base de datos local se excluye con `path="."`, compatible con FullBackupContent.
- `data_extraction_rules.xml`: misma corrección en cloud-backup y device-transfer.
- GitHub Actions: Lint debug ya no bloquea la creación del APK; el informe completo se imprime y se sube como artifact.
- Los errores reales de tests, Kotlin/Java, recursos o `assembleDebug` siguen haciendo fallar la build.
